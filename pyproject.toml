[project]
name = "gluroo-google-health-sync"
version = "0.1.0"
description = "Home Assistant Gluroo to Google Health API integration"
requires-python = ">=3.12"

[project.optional-dependencies]
test = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
